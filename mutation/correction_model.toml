[cosmic-ray]
module-path = "apps/guide_tool/domain/correction_model.py"
timeout = 10.0
excluded-modules = []
test-command = "pytest tests/guide/domain/test_correction_model.py -x -q --no-cov"

[cosmic-ray.distributor]
name = "local"
