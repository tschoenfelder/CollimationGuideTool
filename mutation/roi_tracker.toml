[cosmic-ray]
module-path = "packages/astrotool_core/target/roi_tracker.py"
timeout = 10.0
excluded-modules = []
test-command = "pytest tests/core/target/test_roi_tracker.py -x -q --no-cov"

[cosmic-ray.distributor]
name = "local"
