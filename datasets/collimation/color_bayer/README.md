# color_bayer

Color/Bayer sensor frames, exercising frames/pixel_format.py demosaic handling.

**Status:** placeholder — no frames/expected.json yet. Populated in a later
stage (see PLAN.md). Auto-skip convention: tests reading this dataset must
skip cleanly, not fail, when frames/ or expected.json are absent.
